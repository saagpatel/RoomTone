# DOC-RECONCILIATION

This file was generated by a `/doc-truth-up` documentation-reconciliation pass. The pass treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state. No code was changed and no builds or tests were run. Evidence was gathered by reading source files, manifests, test files, and git history.

---

## Per-Claim Findings

### 1. What It Is

**Status: consistent**
**Evidence: verified-by-reading-code**

README intro and feature list match the implementation:
- LiDAR room scanning: `RoomTone/ARKit/ARSessionManager.swift:56-66`
- Room mode calculator (f(n,m,l) formula): `RoomTone/Physics/RoomModeCalculator.swift:79-84`
- 16-oscillator synthesis: `RoomTone/Audio/OscillatorBank.swift` (16 AVAudioSourceNode instances)
- Positional amplitude: `RoomTone/Audio/ModeAmplitudeController.swift`
- Sabine reverb (RT60): `RoomTone/Model/RoomDimensions.swift:14-17` + `RoomTone/Audio/RoomAudioEngine.swift:82`
- Audio recording: `RoomTone/Audio/AudioRecorder.swift`

No changes made.

---

### 2. Current State

**Status: drifted — fixed**
**Evidence: verified-by-reading-code**

`CLAUDE.md` claimed "Phase 0: Foundation (Weeks 1–2)" in two locations. The actual source tree contains all 23 source files + 4 test files spanning all four phases:
- Phase 0: `RoomToneApp.swift`, `AppState.swift`, `UnsupportedDeviceView.swift`
- Phase 1: `OscillatorBank.swift`, `TimbreProcessor.swift`, `ModeAmplitudeController.swift`, `AudioRecorder.swift`, `RoomAudioEngine.swift`
- Phase 2: `ARSessionManager.swift`, `RoomGeometryProcessor.swift`
- Phase 3: `WireframeOverlayManager.swift`, `StandingWaveOverlay.swift`, `PulseIndicatorView.swift`, `TechnicalOverlayView.swift`, `ARSceneView.swift`
- Phase 4: `OnboardingView.swift`

`HANDOFF.md` corroborates: "Status: Complete (pending device testing)."

**Changes made:**

`CLAUDE.md:24` — *Current Phase* section:
> Before: `**Phase 0: Foundation (Weeks 1–2)**`
> After: `**All phases complete — pending device testing on LiDAR hardware**`

`CLAUDE.md:56-57` — *Portfolio Context › Current State* section:
> Before: `**Phase 0: Foundation (Weeks 1–2)**` + roadmap reference
> After: `**All phases complete — pending device testing on LiDAR hardware**` + HANDOFF.md reference

`CLAUDE.md:82-84` — *Portfolio Context › Next Recommended Move* section:
> Before: generic "capture a dedicated handoff" instruction (HANDOFF.md already exists)
> After: concrete "run on LiDAR device → TestFlight → App Store; see HANDOFF.md"

---

### 3. Stack

**Status: consistent**
**Evidence: verified-by-reading-code**

- Swift 5.10: `project.yml:9` (`SWIFT_VERSION: "5.10"`)
- iOS 17.0 minimum: `project.yml:5`
- Xcode 16.0: `project.yml:7`
- SwiftUI + @Observable: `ARSessionManager.swift:10`, `RoomAudioEngine.swift:4`
- ARKit: `ARSessionManager.swift:1` (`import ARKit`)
- SceneKit (ARSCNView): `ARSessionManager.swift:2` (`import SceneKit`)
- AVFoundation (AVAudioEngine): `RoomAudioEngine.swift:1` (`import AVFoundation`)
- No third-party dependencies: `project.yml:43` (`dependencies: []`)

No changes made.

---

### 4. How to Run

**Status: consistent**
**Evidence: verified-by-reading-code**

README Quick Start instructs `xcodegen generate && open RoomTone.xcodeproj`. `project.yml` exists at repo root confirming XcodeGen is the build system. The `NSCameraUsageDescription` exact text in `project.yml:28` matches the text mandated by `IMPLEMENTATION-ROADMAP.md` Security section.

No changes made.

---

### 5. Known Risks

**Status: consistent / unverifiable**
**Evidence: partially verified-by-reading-code**

CLAUDE.md "Do NOT" constraints are architectural guidelines rather than empirical claims. Spot-checked against code:
- No RealityKit imports anywhere in `RoomTone/` ✓
- ARKit session runs via `ARSCNView` on main thread ✓
- No microphone references anywhere in source (`grep microphone`: no matches) ✓
- No `AVAudioEnvironmentNode` usage ✓

Render-callback allocation policy (`unverifiable` — would require reading every render callback in `OscillatorBank.swift` in full detail, but spot-check shows pre-allocated `[Float]` pattern in use).

No changes made.

---

### 6. Next Move (demonstrably incomplete items)

**Status: consistent**
**Evidence: verified-by-reading-code**

`HANDOFF.md:21-26` lists the concrete remaining steps:
1. Set `DEVELOPMENT_TEAM` (already set in `project.yml:41`)
2. `xcodegen generate` + deploy to physical LiDAR device
3. Full user journey test (onboard → scan → audio → walk → timbres → record → share)
4. App Store screenshots (1290×2796) + ≤30s preview video
5. TestFlight beta across room types
6. App Store submission

These are consistent with the code state (all implementation done; device QA + submission pipeline pending).

No changes to allowed files needed. CLAUDE.md "Next Recommended Move" updated above (claim 2) to reflect these concrete steps.

---

## Contradictions for Manual Review

### docs/PORTFOLIO-DISPOSITION.md — microphone claim (fixed in docs/ per edit rules)

`docs/PORTFOLIO-DISPOSITION.md` described the app as using "**microphone capture** to measure the room's acoustic response." The codebase contains no microphone usage whatsoever:
- `grep -r "microphone|AVCapture|NSMicrophoneUsage" RoomTone/` → no matches
- `IMPLEMENTATION-ROADMAP.md` Security section explicitly states: "**No microphone.** All audio is synthesized. `NSMicrophoneUsageDescription` must NOT be added."
- `PrivacyInfo.xcprivacy` declares only `NSPrivacyAccessedAPICategoryUserDefaults` — no microphone entry.

This was a factual error in `docs/PORTFOLIO-DISPOSITION.md` (a file I am allowed to edit). Fixed in three locations:
- "Current state" paragraph: removed "microphone capture" language
- Unblock trigger item #3: removed `NSMicrophoneUsageDescription` task, replaced with a note not to add it; renumbered items 4–7 → 3–6
- Portfolio operating system table: replaced "Microphone usage description clarity" row with a note that no microphone is used

### project.yml:38 — bundle ID typo (code file, cannot edit)

`project.yml:38` sets `PRODUCT_BUNDLE_IDENTIFIER: com.romtone.app` (missing first 'o' — "romtone" not "roomtone"). `docs/PORTFOLIO-DISPOSITION.md:152` records the Bundle ID as `com.roomtone.app`.

One of these is wrong. The App Store Connect record determines which is authoritative. A human must reconcile the `project.yml` typo against the App Store Connect bundle ID before submission.

**Fix:** If the App Store Connect record uses `com.roomtone.app`, correct `project.yml:38` and `project.yml:57` (tests target) to match. If ASC uses `com.romtone.app`, update `docs/PORTFOLIO-DISPOSITION.md:152` accordingly.

---

## Footer

Generated: 2026-05-30 22:47:30 PDT
Branch: `docs/truth-up-2026-05-30`
HEAD reconciled against: `9ddd6f15139a4d9a683f4127732e381925b5e95e`
