# DOC-RECONCILIATION

This file was generated by a `/doc-truth-up` documentation-reconciliation pass. The pass treats the code as read-only ground truth and edits only documentation so it reflects the repo's actual current state. No code was changed and no builds or tests were run. Evidence was gathered by reading source files, manifests, test files, and git history.

---

## Per-Claim Findings

### 1. What It Is

**Status: consistent**
**Evidence: verified-by-reading-code**

README intro and feature list match the implementation:
- LiDAR room scanning: `RoomTone/ARKit/ARSessionManager.swift:56-58` (`config.sceneReconstruction = .mesh`, `config.planeDetection = [.horizontal, .vertical]`)
- Room mode calculator (f(n,m,l) formula): `RoomTone/Physics/RoomModeCalculator.swift:79-84`
- 16-oscillator synthesis: `RoomTone/Audio/OscillatorBank.swift:10` (`static let oscillatorCount = 16`; 16 `AVAudioSourceNode` instances created)
- Positional amplitude: `RoomTone/Audio/ModeAmplitudeController.swift` (exists; proximity boost logic tested in `RoomToneTests/ModeAmplitudeControllerTests.swift`)
- Sabine reverb (RT60): `RoomTone/Model/RoomDimensions.swift:14-18` (`estimatedRT60` Sabine formula; tested in `RoomToneTests/RoomDimensionsTests.swift:76-86`)
- Audio recording: `RoomTone/Audio/AudioRecorder.swift` (exists)

No changes made.

---

### 2. Current State

**Status: consistent**
**Evidence: verified-by-reading-code**

CLAUDE.md and its Portfolio Context section both read "**All phases complete — pending device testing on LiDAR hardware**". This was corrected from the stale "Phase 0: Foundation" text in a prior pass (`441bfaf`) and remains accurate.

Evidence confirming all phases complete:
- All 23 source files present across all four phases (verified by directory listing)
- 4 test files, 68 tests across 4 suites (`RoomModeCalculatorTests` 19, `ModeAmplitudeControllerTests` 17, `RoomGeometryProcessorTests` 20, `RoomDimensionsTests` 12 — counted from source)
- `HANDOFF.md:3` reads "Status: Complete (pending device testing)"

No changes made.

---

### 3. Stack

**Status: consistent**
**Evidence: verified-by-reading-code**

- Swift 5.10: `project.yml:9` (`SWIFT_VERSION: "5.10"`)
- iOS 17.0 minimum: `project.yml:4-5`
- Xcode 16.0: `project.yml:6`
- SwiftUI + @Observable: `RoomTone/ARKit/ARSessionManager.swift:10` (`@Observable final class ARSessionManager`)
- ARKit: `RoomTone/ARKit/ARSessionManager.swift:1` (`import ARKit`)
- SceneKit (ARSCNView): `RoomTone/ARKit/ARSessionManager.swift:2` (`import SceneKit`); `sceneView: ARSCNView` at line 15
- AVFoundation (AVAudioEngine, AVAudioSourceNode): `RoomTone/Audio/OscillatorBank.swift:1` (`import AVFoundation`)
- simd: `RoomTone/ARKit/ARSessionManager.swift:227` (`simd_float3(arPos.x, arPos.z, arPos.y)`)
- No third-party dependencies: `project.yml:43` (`dependencies: []`)

No changes made.

---

### 4. How to Run

**Status: consistent**
**Evidence: verified-by-reading-code**

README Quick Start (`xcodegen generate && open RoomTone.xcodeproj`) is correct: `project.yml` exists at repo root confirming XcodeGen is the build system, and `RoomTone.xcodeproj/` directory exists.

CLAUDE.md `Build / Test / Run` section correctly states `xcodebuild test` for running the test suite.

Note: the repo-root `Makefile` targets (`swift build`, `swift test`, `swift run`) use Swift Package Manager commands, but there is no `Package.swift` — this is an Xcode project. These Makefile targets will not work as written. The Makefile is not in the editable-docs list; see **Contradictions for Manual Review** below.

No changes made to README.md or CLAUDE.md.

---

### 5. Known Risks

**Status: consistent / unverifiable**
**Evidence: partially verified-by-reading-code**

CLAUDE.md Constraints section architectural guards, spot-checked against code:
- No RealityKit imports: confirmed (no `import RealityKit` anywhere in `RoomTone/`)
- ARKit on main thread: `RoomTone/ARKit/ARSessionManager.swift:200-204` — all state mutations dispatched via `DispatchQueue.main.async`
- No `AVAudioEnvironmentNode`: confirmed absent
- No microphone: `RoomTone/Resources/Info.plist` has no `NSMicrophoneUsageDescription`; `PrivacyInfo.xcprivacy` declares only `NSPrivacyAccessedAPICategoryUserDefaults`
- Render-callback allocation: `RoomTone/Audio/OscillatorBank.swift:24-40` — pre-allocated `UnsafeMutablePointer<Float>` buffers; lock (`os_unfair_lock`) held only for the pointer swap, never inside the callback (`unverifiable` in full depth — spot-check consistent)

No changes made.

---

### 6. Next Move

**Status: consistent**
**Evidence: verified-by-reading-code**

HANDOFF.md lists concrete remaining steps (device test → TestFlight → App Store submission). CLAUDE.md Portfolio Context "Next Recommended Move" matches. No implementation is pending in the source tree — all 23 source files are present and all four phases are complete.

No changes made.

---

## Contradictions for Manual Review

### Makefile:1-11 — SPM commands in an Xcode project (cannot edit)

`Makefile` defines `build: swift build`, `test: swift test`, `run: swift run`. This is an Xcode project with no `Package.swift`; these commands will fail. The correct build commands are documented in `README.md` (`xcodegen generate`) and `CLAUDE.md` (`xcodebuild test`).

**Fix:** Update `Makefile` to use `xcodebuild` equivalents, or remove it. Example:
```makefile
test:
    xcodebuild test -scheme RoomTone -destination 'platform=iOS Simulator,name=iPhone 15 Pro'
```

### IMPLEMENTATION-ROADMAP.md:46-88 — File structure listing is stale (cannot edit)

The architecture file-structure diagram lists files that do not exist and omits files that do:

| Roadmap claims | Actual state |
|---|---|
| `ARKit/LiDARRefinement.swift` | Does not exist; mesh bounding-box logic lives in `ARSessionManager.computeMeshMaxY()` |
| `Views/AROverlayView.swift` | Does not exist; actual file is `Views/ARSceneView.swift` |
| Test directory lists 2 files | Actual test directory has 4 files (`ModeAmplitudeControllerTests.swift`, `RoomDimensionsTests.swift` also present) |
| Views: 5 files listed | Actual: 9 files (`OnboardingView.swift`, `PulseIndicatorView.swift`, `StandingWaveOverlay.swift`, `TechnicalOverlayView.swift` also present) |

**Fix:** Update the file-structure diagram in `IMPLEMENTATION-ROADMAP.md` to match the actual directory contents (a human edit, since the file is not in the doc-only editable set).

### project.yml — Bundle ID typo resolved in July 2026 hardening pass

The prior `com.romtone.app` typo has been corrected. The project generator, test target, generated Xcode project, export configuration, Fastlane Appfile, and release documentation now consistently use `com.roomtone.app` (with `.tests` for the test bundle).

**Remaining owner check:** Confirm the App Store Connect record uses `com.roomtone.app` before creating the provisioning profile. No Apple-account mutation was performed in this repository pass.

---

## Footer

Generated: 2026-06-02 19:52:04 PDT
Branch: `docs/truth-up-2026-06-02`
HEAD reconciled against: `bac6d1d7c03901a97de291a0d2852df845487f61`
